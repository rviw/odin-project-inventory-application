<% if (errors && errors.length > 0) { %>
  <section class="card">
    <h3>Please fix the following:</h3>
    <ul class="error-list">
      <% errors.forEach((error) => { %>
        <li><%= error.msg %></li>
      <% }) %>
    </ul>
  </section>
<% } %>
